#!/bin/sh

## main app launcher
rofi -show combi -modes combi -combi-modes "window,drun,run" -show-icons

## dmenu app launcher
# rofi -combi-modes "drun,run" -show combi -display-drun "" -drun-display-format "{name}" -show-icons -theme dmenu

## fullscreen
# rofi -show drun -show-icons -theme ~/.config/rofi/launchpad.rasi
